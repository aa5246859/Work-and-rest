# Goals — 上線與 Google 同步設定

## 第一部分：放上 GitHub（5 分鐘）

1. 打開 github.com，右上角 ➕ → **New repository**
2. Repository name 填 `goals` → 選 **Public** → **Create repository**
3. 進到新 repo，點 **uploading an existing file**，把 zip 解壓縮後的
   全部檔案拖進去（index.html、manifest.webmanifest、sw.js、三個 png）→ **Commit changes**
4. repo 上方 **Settings** → 左邊 **Pages** → Source 選 **Deploy from a branch**
   → Branch 選 **main**、資料夾 **/(root)** → **Save**
5. 等 1–2 分鐘，網址就是：`https://你的帳號.github.io/goals/`
6. 手機打開這個網址 → 加入主畫面，完成

> 之後更新：把新的 index.html 傳上去覆蓋即可，紀錄不會消失。

## 第二部分：Google 登入同步（10 分鐘，做一次就好）

1. 打開 **console.firebase.google.com**，用你的 Google 帳號登入
   → **建立專案**，名稱隨意（例如 goals）→ Google Analytics 可以關掉 → 建立
2. 左邊 **建構 → Authentication** → 開始使用 → **登入方式** 分頁
   → 啟用 **Google** → 儲存
3. Authentication → **設定** 分頁 → **授權網域** → 新增網域：
   `你的帳號.github.io`
4. 左邊 **建構 → Firestore Database** → 建立資料庫 → 位置選 asia-east1（台灣）
   → 以「正式版模式」啟動
5. Firestore → **規則** 分頁，全部換成下面這段 → 發布：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
```

6. 回專案首頁 → 點 **</>**（新增網頁應用程式）→ 暱稱隨意 → 註冊
   → 會看到一段 `const firebaseConfig = { apiKey: "...", ... }`
7. 打開 index.html，搜尋 `FIREBASE_CONFIG=null`，
   把 `null` 換成那個 `{...}` 物件 → 存檔 → 重新上傳到 GitHub
8. 打開 app → 紀錄分頁 → **使用 Google 登入** → 完成，之後全自動

## 費用
Firebase 免費額度：每天 5 萬次讀取、2 萬次寫入。
你一個人用，一天用量大概幾十次，永遠免費。

## 疑難排解
- 登入跳出後又回來沒反應 → 檢查第 3 步授權網域有沒有加對
- 顯示「設定有誤」→ 檢查第 7 步貼的物件格式（要是合法的 JS 物件）
- 卡住直接把畫面截圖丟給 Claude
